```sh
B树的版本为：所有的数据头存储在树叶上，也就是非叶子节点的索引值均可以在叶子节点中找到, B树的阶在
构造时作为参数传入。
这里实现的版本为Robert Sedgewich所提供的版本, 并非Mark Allen Weiss和国内教科书作者如严蔚敏等
所提供的版本。

该版本的特点为：（B树的阶为M）
1. 所有的数据均存储在叶子节点上，接近B+树的结构。
2. 当非叶子节点内的元素个数为m个时，那么它下级的节点个数也是m个，而不是类似 2-3-4树那样定义的m+1
   个节点。
3. 节点内元素中存储的值为下级各节点中存储值的最小值，而不是最大值。
5. 存在叶子节点的情况下，根节点的元素最小值为2，其他节点的元素最小值为M/2（向上取整）到M。如3阶非根节点
   的元素数为2-3个，4阶非根节点的元素数为2-4个，5阶非根节点的元素数为3-5个。

下面是一棵3阶B树的示意图：

                                10-63
                            /              \
                    10-21-51              63-81
                /     |     \             /    \
            10-15  21-37-44   51-59     63-72   81-91-97


本文件夹 balance_tree_debug 内，实现了B树4个版本的代码，分别是

1. 没有parent_指针的递归插入，删除版本
2. 没有parent_指针的非递归插入，删除版本
3. 携带parent_指针的递归插入，删除版本
4. 携带parent_指针的非递归插入，删除版本

```

```sh
版本说明：

    balance_tree_debug 文件夹中实现b树所有的版本的算法代码，但为了便于调试，内部数据结构定义没有
采用最优方式，而且使用了一种 gdb下查看内存更直观的方式。所以文件夹后缀携带有 `_debug`. 和 
balance_tree 文件中的版本相比，算法逻辑完全一致。
    如果实际使用，建议使用 balance_tree 文件夹中的代码。 因为 balance_plus 文件夹中的非递归版本增
加了c++类常用的构造函数和赋值运算符重载函数，而且分别实现了普通版本以及右值引用的版本。

    --Yankefei
```