```sh

本文件夹 binary_rb_tree 内，实现了红黑树4个版本的代码，分别是:

1. 没有parent_指针的递归插入，删除版本
2. 没有parent_指针的非递归插入，删除版本

3. 携带parent_指针的递归插入，删除版本
4. 携带parent_指针的非递归插入，删除版本

其中4版本的代码中额外实现了c++的四组拷贝构造函数以及赋值运算符重载函数（含右值引用版本），因为携带parent_
指针，可以做到无堆栈协助地遍历整棵树，所以作为tools 目录中map和set容器的一种实现。

    --Yankefei
```